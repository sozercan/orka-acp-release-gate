# Orka example validation fixture

A small arithmetic module for live workflow validation. Run `node --test`.
No third-party packages or network access are required for the tests.

Validation rerun after the Kubernetes service-account defaulting fix.

Validation rerun after the MCP polling and publication recovery fixes.

Validation rerun after the validation container termination-path fix.
